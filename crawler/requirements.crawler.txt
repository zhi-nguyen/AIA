# Minimal dependencies for the News Crawler service.
# Intentionally excludes FastAPI, psycopg2, langchain-graph, audio libs, etc.

# ── Celery + broker ────────────────────────────────────────────────────────
celery==5.3.6
redis==5.0.3

# ── Google Cloud / Vertex AI ───────────────────────────────────────────────
google-cloud-aiplatform>=1.38.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.0

# ── LangChain (Vertex AI + core only) ─────────────────────────────────────
langchain-core>=0.1.0
langchain-google-vertexai>=1.0.0

# ── Pydantic & Config ─────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.7.0

# ── LlamaIndex & Vector Store (Needed for user context) ───────────────────
llama-index
llama-index-vector-stores-postgres
llama-index-embeddings-google
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.30

# ── Google SDKs ───────────────────────────────────────────────────────────
google-genai
google-cloud-discoveryengine>=0.13.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.28.0
beautifulsoup4>=4.12.0
feedparser>=6.0.0
